## Summary



## Checklist

- [ ] Ran `runtests` locally in MATLAB (e.g. `run('tests/run_all_tests.m')`) -- all tests pass (paste summary below)
- [ ] If this PR targets `main`: bumped the `Version` line in `src/Contents.m`

## Local test run summary

```
(paste runtests output here)
```
