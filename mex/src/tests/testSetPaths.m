%%esto esta vacio de momento solo lo dejamos para mantener estructura
function testSetPaths()
% testSetPaths is a template function that only adds the path to all
% necesary external references to a given set of code

%restore path as indicated in pathdef.m
matlabpath(pathdef);
addpath('..\deploy');